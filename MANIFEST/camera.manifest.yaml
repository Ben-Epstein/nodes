COMMAND:
  - name: Camera
    key: CAMERA
    type: LOADER
    docker:
      folder_name: CAMERA
      rq_name: camera
      arguments:
        linux:
          - --rm
          - --privileged
          - --env='DISPLAY'
          - --volume='/tmp/.X11-unix:/tmp/.X11-unix:rw'
          - --device='/dev/video0:/dev/video0'
    parameters:
        file_type: { type: "string", default: "image" }
        camera_ind: { type: "integer", default: 0 }
